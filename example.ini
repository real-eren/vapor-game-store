# an example of a db credentials file
# actual credential files should be excluded from VCS
# and distributed through other, secure means
address=localhost
db=university
username=dbuser
password=scsd431134dscs
# timeout is optional, default 30
timeout=10
